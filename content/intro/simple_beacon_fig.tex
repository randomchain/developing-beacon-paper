\begin{figure}[htb]\pgfdeclarelayer{background}
    \pgfsetlayers{background,main}
    \centering
    \footnotesize
    \begin{tikzpicture}[auto]
        \node[blockdef] (input) {Input\\source};
        \node[blockdef, right=1cm of input, text width=7em] (computation) {Computation};
        \draw[arrow] (input) -- (computation);
        \node[blockdef, right=1cm of computation] (users) {Users};
        \draw[arrow] (computation) -- (users);
        % \begin{pgfonlayer}{background}
        %     \node[block] (beacon) [container, fit={([shift={(0mm,-2mm)}]inp.north) (computation) ([shift={(0mm,2mm)}]out.south)}] {};
        % \end{pgfonlayer}
    \end{tikzpicture}
    \caption{Abstract randomness beacon}\label{fig:abstract_beacon}
\end{figure}
